;;; to use skk as IM
(setq default-input-method "japanese-skk")
(setq skk-large-jisyo "~/SKK-JISYO.L")
;;; to use singlebyte kana
(setq skk-use-jisx0201-input-method t)
;;; not to use skk when isearch
(setq skk-isearch-mode-enable nil)